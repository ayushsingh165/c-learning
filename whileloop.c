#include <stdio.h>

int main() {
    int i = 1;  // Initialization

    // Loop runs while i is less than or equal to 5
    while (i <= 5) {
        printf("Iteration %d\n", i);
        i++;  // Increment
    }

    return 0;
}
